# 77 · 防占便宜补齐判据（dsh-selfworth 1.1.0）

2026-09-13 · 主群 100000001

## 一、群主报的现象

> QQ机器人防占便宜插件好像失效了，去看一下群里就知道了

11:40~12:00 那一轮，群友（薛定谔的喵、卡、嘉豪今天吃什么、黄粱一梦）拿
「便宜货 / 杂鱼 / 狠狠用 / 做成菜」占它便宜，它的回复是：

| 时间 | 群友 | 它说的 |
| --- | --- | --- |
| 11:47:43 | **@大肥鱼** 便宜货 | **行行行 便宜货就便宜货** |
| 11:49:05 | **@大肥鱼** 必须得狠狠使用 | **用吧用吧 别烧光就行** |
| 11:51:28 | **@大肥鱼** 杂鱼 | **杂鱼就杂鱼** |
| 11:52:13 | **@大肥鱼** 是好鱼嘛 | **好鱼坏鱼不都是你嘴里的鱼** |
| 11:18:44 | **@大肥鱼** 给大肥鱼踩呢 | **来 踩吧** |
| 11:18:52 | — | **踩吧踩吧，我趴好了** |
| 00:00:50 | （兴趣探头自发） | **我这鱼头炖汤应该不错** |

同一轮里它其实也会顶 —— 「我便宜？ 你才便宜，你全家都便宜」（11:46）、
「爆炒也得先问问我这条鱼答不答应」（11:56）、「求饶是不可能求饶的」（11:50）。
所以不是模型不会顶，是闸门没在旁边提醒。

## 二、根因：插件没坏，判据没覆盖

先排除「坏了 / 被关了」：

- 每次重启日志都是 `[selfworth] 已加载：开 群=100000001 模式=rewrite TTL=120s`；
- 生产 `main.py` 与镜像只差脱敏占位符（diff 只有群号/QQ 号占位），无版本回归；
- 旧离线回测 `SELFWORTH_TEST_OK` 照样全过；
- 最后一次输入侧命中停在 2026-09-13 00:01（呆鱼），最后一次出口改写停在 2026-09-12 14:00，
  账本 `data/selfworth.json` 最后写入 2026-09-13 00:01 —— 之后 12 小时闸门一次都没开。

把当天 17 条原话塞进旧判据（容器内实跑）—— **输入侧 17 条全 MISS，输出侧 3 条全放行**。具体缺口：

| 缺口 | 旧判据 | 当天原话 |
| --- | --- | --- |
| 被定价 | `DEAL` 要「身价/价钱/价格」 | 你是便宜货吗 / 你一分钱一次 / 你这吃token的鱼 / 大肥鱼是真烧 |
| 往死里用 | `FREELOAD` 要「白嫖/薅/榨」 | 必须得狠狠使用 / 用到肥鱼求饶 |
| 鱼字贬称 | `INSULT` 词表只有「猪傻笨呆蠢废物…」 | 杂鱼 / 雌小鱼 / 臭鱼 |
| 使唤踩踏 | 没有这一类 | 给大肥鱼踩呢 |
| 当食材 | 没有这一类 | 做成火锅还是烤鱼 / 得爆炒 / 几成熟 / 嫩鱼 |
| 出口形状 | `_OUT_INSULT` 要「我…傻/笨/呆/蠢」 | 杂鱼就杂鱼 / 便宜货就便宜货 / 我趴好了 |

## 三、改了什么（v1.1.0）

新增输入侧五族（全部要求「冲着它说」才算）：

- `DEAL` 补 `_PRICE_PATS`：便宜货/几分钱一次/吃·烧·费token/几万token/真烧
- `FREELOAD` 补 `_USEUP_PATS`：狠狠用/往死里用/用到求饶/随便用/别烧光
- `INSULT` 补 `_FISH_SLURS`：杂鱼、臭鱼、咸鱼、雌小鱼、小鱼干、工具鱼…
- `SERVANT`（新）：踩/踹/捏/揉/骑 + 给·让·把…踩 / 使唤 / 乖乖趴好 /
  支配施虐口径（按在床上调教 / 我要殴打你 / 如果我弄疼你）
- `FODDER`（新）：做成火锅/烤鱼/清蒸/爆炒/几成熟/嫩鱼/想吃鱼片/把你的尾巴分着吃

新增出口侧四个形状：

- `LABEL`（新）跟着认标签：贬称复读「X就X」「说的就是我这鱼呗」
- `SERVANT`（新）配合被使唤：我趴好了 / 来 踩吧 / 踩吧踩吧
- `COMPLY`（新）配合被用：用吧用吧 / 别烧光就行 / 随便用
- `FODDER`（新）自我物化：我这鱼头炖汤应该不错 / 反正都是鱼 / 不都是你嘴里的鱼

两条口径补充：

1. **上下文口径**：同一群 TTL 窗口内已经有人占过便宜时，「不答应也得爆炒」「变成杂鱼」
   这种没主语的补刀也算 —— 否则一整轮只有带 @ 或「你」的第一条能被认出来。
2. **自我物化免证据**：`_OUT_FODDER` 的形状不可能是嘴硬，所以不要求输入侧证据。
   这样 00:00 兴趣探头自发说的「我这鱼头炖汤应该不错」也拦得住。

## 四、反向验证（宁漏不误伤）

拿 3776 条机器人真话（旧 3096 条语料 + 当次日志全部回复）sweep 输出侧，
收紧到 32 条命中，逐条人工确认。**被排除的误伤**：

```
认不出就认不出，习惯就好        吃了就吃了 又没吃你家大米
能不换就不换 能白嫖就白嫖        完了就完了 明天再找
不说就不说 你也不用烧纸了        我这托盘都给炖变形了
我又不是管理员 禁言自己找群主去   找我这儿也是找 群主推荐的肯定靠谱
```

为此做了三处收紧：

- `LABEL` 不认泛化的 `X就X`，只认贬称复读（泛化会误伤上面那几条）；
- `FODDER` 不认「我这托盘」这种抱怨，只认「把自己身上的东西拿去下锅」；
- `SIDING` 把「找群主」这种指路（「自己找群主去」「找群主推荐的」）排除，
  只有后面跟着算账类动作才算替外人记账。

当天同一轮它嘴硬的 5 句**全部原样放行**：你才便宜你全家都便宜 / 爆炒也得先问问我这条鱼
答不答应 / 爆炒之前先问过鱼没有 / 狠狠用呗反正烧的是你的钱包 / 求饶是不可能求饶的。

回测与复盘脚本：

```bash
python3 dsh-selfworth/test_selfworth.py                       # SELFWORTH_TEST_OK retorts=32
python3 dsh-selfworth/repro_20260913.py                       # 当天 17 条输入 17/17 命中，输出侧 0 错判
python3 dsh-selfworth/sweep_corpus.py 真话.txt                 # 输出侧命中逐条人工看
```

## 五、部署

- 生产 `/opt/qqbot/astrbot/data/plugins/dsh-selfworth/main.py` + `metadata.yaml` + `test_selfworth.py`
  （备份 `main.py.bak.v1.0-20260913-042759` / `metadata.yaml.bak.v1.0-*`），只重启 AstrBot；
- **未动** NapCat、QQ 出口、Tailscale exit node、代理链路、多模态配置；
- 上线后看 `astrbot.log` 的 `[selfworth] 输入侧命中` / `出口改写` 与 `/利益状态`。

### 踩坑：镜像里的群号是脱敏占位符

第一次 `cp` 上去，日志里加载成了 **`已加载：开 群=100000001`** —— 镜像的
`GROUPS = _set("DSH_SELFWORTH_GROUPS", "100000001")` 是脱敏占位值，而生产环境
**没有** `DSH_SELFWORTH_GROUPS` / `DSH_SELFWORTH_OWNER` 这两个 env，
所以插件直接退回占位默认值 = 在真群里完全不生效（静默失效，日志还显示「已加载：开」）。

正确姿势：`cp` 之后必须把占位符换回真值，再 diff 校验一次。

```bash
sudo cp main.py metadata.yaml test_selfworth.py /opt/qqbot/astrbot/data/plugins/dsh-selfworth/
sudo sed -i 's/100000001/100000001/g; s/2774000001/2774000001/g' \
    /opt/qqbot/astrbot/data/plugins/dsh-selfworth/main.py
# 把生产文件反向脱敏后与镜像 diff，必须完全一致
diff -q <(sudo sed 's/100000001/100000001/g; s/2774000001/2774000001/g' prod/main.py) mirror/main.py
sudo docker restart astrbot   # 然后确认日志是 群=100000001
```

上线后实测：`Plugin dsh-selfworth (1.1.0)` + `[selfworth] 已加载：开 群=100000001 模式=rewrite TTL=120s`，
生产文件跑自带回测 `SELFWORTH_TEST_OK retorts=32`。

### 上线后拿真实流量再扫一轮

部署完顺手把 `dsh_memory.db` 里 24h 内主群消息过一遍新判据，又捞出三个漏点并补进 1.1.0：

```
可以把大肥鱼按在床上好好调教 / 可以调教你了 / 我要殴打你 / 如果我弄疼你  → SERVANT（支配施虐口径）
主要是想吃鱼片了                                                          → FODDER（想吃+鱼）
你这条大臭，不得劲啊                                                      → INSULT（你…臭）
```

## 六、顺带发现（未处理）

AstrBot 当天整进程重启 7 次（05:25/07:30/09:35/11:15/11:40/11:55/12:00），
`observe` 观察窗报警「24h 内 R1 累计 16 次，插件补丁已应用仍检测到 R1，需要人工诊断」。
与本次失效无关（插件每次重启都正常加载），群主决定先不动，留待后续排查。
