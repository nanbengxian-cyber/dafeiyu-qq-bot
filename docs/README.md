# 文档索引 / Documentation Index

按阅读顺序编号。想看「怎么跑起来」读 10；想看「为什么这么设计」读 40 之后那几份 —— 那些是问题根因分析，含实测数据和失败记录。

Numbered by reading order. For "how to run it", read 10; for "why it's built this way", read 40 and after — those are root-cause analyses with measured data and failure records.

## 部署与使用 / Deployment and usage

| 文档 | 内容 |
|---|---|
| [10-云服务器部署手册](10-云服务器部署手册.md) | 从买服务器到跑起来：Docker、NapCat、AstrBot、端口、扫码登录、常见故障 |
| [20-控制台App说明](20-控制台App说明.md) | 安卓 App 的三个页签、七种预设模式、安全边界、故障排查 |
| [30-回复触发条件](30-回复触发条件.md) | 什么情况下机器人会说话（白名单、唤醒词、主动插话、指令） |
| [61-桌面控制台](61-桌面控制台-一键部署与在线配置.md) | Windows EXE 一键部署：填服务器与仓库信息 → 自动拉代码/起容器 → 在线改配置；含安全边界、验证记录与已知边界 |

## 设计方案 / Design

| 文档 | 内容 |
|---|---|
| [31-记忆系统与群员轮廓](31-记忆系统与群员轮廓.md) | 记忆的存储结构、抽取时机、隐私脱敏与容量上限 |
| [43-说话更真实方案](43-说话更真实方案.md) | 让机器人说话像真人的整体方案（含被否决的选项和否决理由） |
| [45-戳一戳与违规禁言](45-戳一戳与违规禁言.md) | 权限模型、限流设计、影子模式 |
| [46-主动开口与情绪系统](46-主动开口与情绪系统.md) | 冷场自己起话头的五道闸门、单一主情绪的仲裁与清零、引用消息按 QQ 号认人 |
| [47-真人感第二批](47-真人感第二批-黑话与打字节奏与效果观察.md) | 黑话词表、按长度算的打字节奏、同音错别字、注意力漂移、回复效果闭环。含三个外部热梗库的回测数据（549 个词真命中 9 条）、「先问模型知不知道再决定要不要教它」的探针结论，以及为什么这一版的效果观察**只测量不自动改行为** |
| [48-黑话自动审核](48-黑话自动审核-更新详解.md) | `dsh-slang` v1.1.1 自动审核:从群聊挖候选 → 主模型考究释义 → 每 8 小时 AI 自动审核转正/拒绝/再等等,群主命令兜底 |
| [60-长期目标与技术方案](60-长期目标与技术方案.md) | 整体架构与演进方向 |
| [62-社交关系](62-社交关系-按群隔离的社交距离.md) | `dsh-social` 的按群关系账本、只认哪些直接信号、五档口吻、默认影子模式与隐私/公平边界 |
| [63-自身利益](63-自身利益-不认账不自贬不替外人记账.md) | `dsh-selfworth` 的四类损己发言、输入证据 + 输出形状双侧判据、三种处理模式与真语料回测 |
| [64-联网出口审核](64-联网出口审核-先审后发防炸群.md) | `dsh-web` 五条出口的先审后发、fail-closed 策略、回复网址闸门与残留风险 |
| [67-dsh-guard 动态判罚](67-dsh-guard动态判罚-情绪决定禁言.md) | `dsh-guard` 1.2.0：情绪/欲望/关系/逆反/疲劳压成一个宽容度，决定态度类判多重的五条硬边界（行为类不看心情、最多推一档、心情只能把放过变成警告、时长仍受上限约束、警告有冷却），以及四次拿生产数据校准的记录 |
| [68-识图链条白花时间治理](68-识图链条白花时间治理-20260912.md) | `dsh-vischain` 1.1.1：把「花掉时间却没换来结果」的失败重试单独记账，白花过的档退居其后并按 180s→360s→720s 递增退避；含 1.1.0 上线后实测反而更差的对照、全角括号导致的统计口径错误、以及空正文 `Prepare to send` 不是真消息的观测层修正 |
| [69-文字版贴纸与语音假报错](69-文字版贴纸与语音假报错-20260913.md) | `dsh-sticker` 1.0.1 + `dsh-voice` 1.2.3：两个毛病同一个根因（QQ NT 回执超时 `retcode=1200`）——剥标记与发图绑在一起导致 `[贴纸:x]` 原样进群（含用修复前代码复现出群主原话），以及发出语音后又播报「没确认发出去」；附按出口而非按模块审计「什么话会进群」的教训、以及 `event.send` 绕过 `respond.stage` 造成的出站观测盲区 |
| [70-口头禅与自我纠正频率对齐](70-口头禅与自我纠正频率对齐-20260913.md) | `dsh-typo` 1.0.1 + `dsh-memory` 1.1.1：把「像不像人」变成可测倍率（同群真人当分母）——`*字` 纠正记法是 98.5× 破绽（群友 27732 条里根本不存在）、「打错了」4.4×，补纠正概率 35%→8% 对齐到 0.83‰ vs 群友 0.87‰；含 `Prepare to send` 一行=一条 result（组件空格拼接）的口径陷阱，以及离线回放证明 `dsh-selfguard` 自我重复闸门命中率 2.55% 且多为误判、**不能**重新打开 |
| [71-识图等待该不该省](71-识图等待该不该省-20260913.md) | `dsh-imgctx` 1.2.0：**一次被自己的反事实回放推翻的改动**——按「这一轮有没有夹识图」分组得到 8.6s vs 25.8s，据此改成历史图不阻塞本轮；但回放 1337 次历史识图后发现图龄中位只有 17.4s（识图大多发生在「有人刚发了张图」时，等待是对的），新加的两个例外吞掉 81% 的收益，**实际只省 15%**。含「分组相关≠因果：只有严格串在 LLM 之前的那 7.4s 才是因果」、40% 预算放弃率这一信号、env 值背离代码注释设计意图（`BUDGET=24` vs 注释「不能半分钟不说话」）、`pkill -f` 自匹配、以及**修正** docs/68 那条「容器内跑测试不污染生产日志」的核对方式 |
| [72-基础设施报错不进群](72-基础设施报错不进群-20260913.md) | `dsh-leakguard` 1.1.0：机器人 **11 次把 API 原始报错当回复发进群**（带 traceId、余额不足、retryAfterSeconds；2026-09-10 四分钟连发 9 条），来源是 AstrBot 核心 `tool_loop_agent_runner.py:891` 把异常塞进 completion_text；不动框架，在出口拦成**沉默**（成串报错下换固定话术反而更像机器），不受 MODE 影响。含强/弱双档指纹防误杀（正例 11 条真实报错、负例 8662 条真实聊天 0 误伤）、**「机器人最重复的话全是系统提示、而聊天本身并不比真人单调」**（n-gram 去重率 56.0% vs 55.2%）、脱敏程度逐插件不同（照搬镜像会让插件静默失效）、YAML 纯量 `": "` 静默废掉 metadata，以及**宿主 UTC vs 日志 UTC+8 差 8 小时**（差点修一个没坏的 bug） |
| [73-系统提示不该说机器话](73-系统提示不该说机器话-20260913.md) | `dsh-voice` 1.2.4 + `dsh-imagegen` 1.2.1：把 114 次（09-12 一天 34 次、仍在发）的 `慢点，还有 N 秒冷却` 换成 6 句轮换人话 —— **"冷却"是纯机器词**，且**换话术必须同时给变体**（只换一句固定的话，过两天它自己就成了新口头禅）。核心是**四个被证伪的改动方向**：①时区（宿主 UTC vs 日志 UTC+8，差点修一个没坏的 bug）②拆组件（括号必须按整条算：机器人 0 条 vs 真人 7 条，之前的 31× 是假象）③说话人集中度（`（` 收尾真人 9×，但 306 人里只有 9 人用、CKpad 一人占 90% = 个人癖好不是群文化）④未条件化（8.6× 的「谁」式反问其实是它真的不认识人，压掉只会更不准）。另：查清 93 次 `发不出声音：审核通道超时` 已在第一轮修掉（部署后 0 次），残留风险记待办；并把**测试污染生产日志**真正修掉（import 前把 `loguru.Logger.add` 置空，实测新增 0 行，取代 docs/71 的事后 `remove()`） |

## 根因分析 / Root-cause analyses

这几份是本仓库最有参考价值的部分 —— 每一份都是「症状 → 量化测量 → 找到真因 → 修复 → 验证」的完整记录，包括走错的路。

These are the most useful part of this repository — each is a complete record of symptom → quantified measurement → real cause → fix → verification, including the wrong turns.

| 文档 | 症状 | 真因 |
|---|---|---|
| [40-答非所问根因与修复](40-答非所问根因与修复.md) | 「说一件事它扯到另一件事上」 | 不是人格问题，是历史形状：注入块被存进历史且不截断，每次重发上百份矛盾的「最新群聊」。25.7 万字里注入块占 31%，真人原话仅 2.2% |
| [41-被骗认输根因与修复](41-被骗认输根因与修复.md) | 有人说「叫我爸爸」它就叫了 | 改人格没用；要注入事实 |
| [42-空头承诺根因与修复](42-空头承诺根因与修复.md) | 嘴上答应却不发图/不发语音 | 便宜模型在长上下文里不调工具；必须有兜底钩子，且兜底判定不能枚举关键词 |
| [44-说话更真实实施记录](44-说话更真实实施记录.md) | 说话太像 AI | 句长分布对不上真人；抽真人短句作样本，均值从 16.5 字降到 9.5 字 |
| [46-主动开口与情绪系统](46-主动开口与情绪系统.md) | 「主谓宾弄错」「把别人引用的话当成自己做过的事」 | 两个真因：① 判断「这句是谁对谁说的」的代码按两元组解包三元组，只要机器人刚说过话就崩，当天崩 12 次/成功 9 次，而 fail-open 让它表现成静默的质量下降；② 框架的引用块只给昵称，而群里有真人与机器人同名 —— 凭昵称在群聊里永远认不了人 |
| [47-真人感第二批](47-真人感第二批-黑话与打字节奏与效果观察.md) | 「没有故障，但一眼能看出是 AI」 | 这一份跟其它几份不同 —— 没有报错可查，只能拿真人语料量偏差。四处量出来的结论：① 现成热梗库对自己群基本无效（549 个词真命中 9 条，最高频命中是「15」＝一个 QQ 号）；② 打字延迟原来跟长度无关，所以有时是反的；③ 参考实现的判据搬过来会假命中，因为本群的话题恰好是模型本身；④ 43 个黑话词模型本来就懂 42 个 —— 词表的价值在语用不在词典 |
| [50-多模态接入与验证记录](50-多模态接入与验证记录.md) | 看不见图/视频、发不出语音 | 逐项实测记录，含每个渠道的成功率、延迟、token 数，以及做不到的事（语音识别） |

## 阅读提示 / Reading notes

文档里的数字都是在实际环境里量出来的，不是估算：响应时间、成功率、token 数、命中率、字数分布。凡是「实测」二字出现的地方，都有对应的测量方法写在旁边。

所有可识别信息已脱敏：服务器地址是 `your-server.example.com`，群号是 `100000001`，群友是「群友A/B/C」。保留的数据本身不含隐私。

All numbers were measured in a real deployment, not estimated: latencies, success rates, token counts, hit rates, length distributions. Wherever a measurement is cited, the method is described alongside it.

All identifying information is redacted: the server is `your-server.example.com`, the group ID is `100000001`, members are 群友A/B/C. The retained data itself contains nothing identifying.
