name: dsh-quoteref
desc: 引用回复。每 N 次「提问式的 @」用一次 QQ 引用回复而不是直接说（默认 N=4，外加 240 秒冷却），频率对齐本群真人实测的 6.0%。引用时把 At 摘掉——真人是引用或艾特二选一。按次数而非概率计数，避免扎堆；框架会把 Reply 抽成 header 只跟第一条发，所以拆句后也只出现一次。
version: 1.0.0
author: 大肥鱼
repo: local
